
/*
	Set of reducers for single state objects
 */
const reduce = (state = {}, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

/*
	Set of reduvers for an array of state objects
 */

const reducers = (state = [], action) => {
  switch(action.type) {

    default:
      return state;
  }
}

export default reducers;
